<div>
  {{#each this}}
  <section class="section">
    <div>
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="flow-text"> 
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>